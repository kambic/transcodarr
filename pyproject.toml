[project]
name = "transcodarr"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "django==6.1",
    "django-htmx>=1.23",
    "django-tasks-db>=0.12",
    "whitenoise>=6.9",
]

[dependency-groups]
dev = [
    "django-types>=0.24.0",
]
