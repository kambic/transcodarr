DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=1
TZ=Europe/Ljubljana

# database | immediate | dummy
TASKS_BACKEND=database

FFMPEG_BIN=ffmpeg
FFPROBE_BIN=ffprobe
TRANSCODE_CACHE_DIR=/var/cache/transcodarr

# Never replace a source file with something larger than this fraction of it.
MAX_OUTPUT_SIZE_RATIO=1.0
